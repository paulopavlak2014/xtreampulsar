services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-xtreampulsar}
      POSTGRES_USER: ${POSTGRES_USER:-xtreampulsar}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-xtreampulsar}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    networks:
      - internal

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-xtreampulsar}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-xtreampulsar}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-15m}
      JWT_REFRESH_EXPIRES_IN: ${JWT_REFRESH_EXPIRES_IN:-7d}
      LICENSE_SERVER_URL: ${LICENSE_SERVER_URL:-http://license:3001}
      LICENSE_KEY: ${LICENSE_KEY:-}
      LICENSE_ENFORCE: ${LICENSE_ENFORCE:-false}
      LICENSE_OFFLINE_GRACE_HOURS: ${LICENSE_OFFLINE_GRACE_HOURS:-72}
      ADMIN_API_KEY: ${ADMIN_API_KEY:-}
      # Bu sunucunun node metrik ucu (/api/v1/node/metrics) icin paylasilan sir.
      # Panelde Sunucular > Sunucu Guvenligi > "API Secret (Node Anahtari)"
      # alanina AYNI deger yazilmalidir; aksi halde CPU/RAM/Disk gosterilemez.
      # Tanimsizsa uc 403 doner (dormant-safe).
      NODE_SECRET: ${NODE_SECRET:-}
      # Destek sistemi — kendi sunucumuzda (panel.xtreampulsar.com) control panel
      # kendi API'mizdir: Docker DNS ile api servisine loopback yerine servis adıyla ulaş.
      CONTROL_PANEL_URL: ${CONTROL_PANEL_URL:-http://api:3000}
      PANEL_LICENSE_KEY: ${PANEL_LICENSE_KEY:-DEV-TEST-KEY}
      SERVER_URL: ${SERVER_URL:-http://localhost}
      CORS_ORIGINS: ${CORS_ORIGINS:-}
      MAX_CONNECTIONS_PER_IP: ${MAX_CONNECTIONS_PER_IP:-0}
      DEMO_MODE: ${DEMO_MODE:-false}
      DEMO_BYPASS_IPS: ${DEMO_BYPASS_IPS:-}
      DEMO_BYPASS_USERS: ${DEMO_BYPASS_USERS:-}
      GUARD_RESTREAM_ENFORCE: ${GUARD_RESTREAM_ENFORCE:-false}
      RS_WINDOW: ${RS_WINDOW:-300}
      SERVER_PORT: ${SERVER_PORT:-25461}
      FFMPEG_PATH: ${FFMPEG_PATH:-/usr/bin/ffmpeg}
      HLS_OUTPUT_PATH: ${HLS_OUTPUT_PATH:-/tmp/xtreampulsar/hls}
      HEALTH_PROBE_USER_AGENT: ${HEALTH_PROBE_USER_AGENT:-}
      MEDIA_PATH: ${MEDIA_PATH:-/data/media}
      ARCHIVE_PATH: ${ARCHIVE_PATH:-/data/archive}
      MIGRATION_DATA_PATH: ${MIGRATION_DATA_PATH:-/data/migrations}
      NODE_ENV: production
    volumes:
      - hls_data:/tmp/xtreampulsar/hls
      - migration_data:/data/migrations
      - backup_data:/opt/xtreampulsar/backups
      - media_data:/data/media
      - archive_data:/data/archive
    restart: always
    expose:
      - "3000"
    networks:
      - internal

  license:
    build:
      context: .
      dockerfile: apps/license/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-xtreampulsar}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-xtreampulsar}
      ADMIN_API_KEY: ${ADMIN_API_KEY:-}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      NODE_ENV: production
      PORT: 3001
    restart: always
    expose:
      - "3001"
    networks:
      - internal

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    depends_on:
      - api
    restart: always
    expose:
      - "80"
    networks:
      - internal

  website:
    build:
      context: .
      dockerfile: apps/website/Dockerfile
    restart: always
    networks:
      - internal

  control:
    build:
      context: .
      dockerfile: apps/control/Dockerfile
    depends_on:
      - api
    environment:
      CONTROL_JWT_SECRET: ${CONTROL_JWT_SECRET:-}
    restart: always
    expose:
      - "3003"
    networks:
      - internal

  nginx:
    image: nginx:alpine
    depends_on:
      - api
      - web
      - control
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - hls_data:/var/www/hls:ro
      - ./nginx/webroot:/var/www/certbot:ro
    ports:
      - "80:80"
      - "443:443"
      - "25461:25461"
      - "25463:25463"
    restart: always
    networks:
      - internal

volumes:
  archive_data:
  postgres_data:
  redis_data:
  hls_data:
  migration_data:
  backup_data:
  media_data:

networks:
  internal:
    driver: bridge
